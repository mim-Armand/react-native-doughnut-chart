/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Donut from './Donut';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Donut);

module.exports = {Donut};
